rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ยอมให้ทุกคน อ่านและเขียนข้อมูลได้ "ทุกตาราง"
    match /{document=**} {
      allow read, write: if true; 
    }
  }
}